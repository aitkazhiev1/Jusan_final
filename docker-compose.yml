version: '3'
services:
  backend:
    build:
      context: ./server
    ports:
      - "8081:8081"
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://database-1-jusan.co9yxdynjyur.us-west-2.rds.amazonaws.com:5432/hackathon
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_JPA_HIBERNATE_DDL_AUTO: update

  frontend:
    build:
      context: ./client
    ports:
      - "3000:3000"

  db:
    image: postgres:13
    ports:
      - "5434:5434"
    environment:
      POSTGRES_DB: hackathon
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus:/etc/prometheus
    command: ["--config.file=/home/yeras/Documents/exam/Jusan_final/prometheus.yml"]

  grafana:
    image: grafana/grafana-enterprise
    ports:
      - "3333:3333"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin

  postgres_exporter:
    image: wrouesnel/postgres_exporter
    ports:
      - "9187:9187"
    environment:
      - DATA_SOURCE_NAME=postgresql://postgres:postgres@db:5432/postgres
